import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: { marginBottom: 6, fontSize: 12, fontWeight: '900', color: '#333' },
  goal: { marginHorizontal: 0, padding: 10 },
})
