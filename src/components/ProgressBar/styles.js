import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  progressBar: {
    height: 12,
    borderRadius: 12,
    backgroundColor: '#999',
    overflow: 'hidden',
  },
})
